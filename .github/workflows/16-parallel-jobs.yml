name: 16 Parallel Jobs
on: workflow_dispatch

jobs:
  lint-backend:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Running backend linting in parallel"

  lint-frontend:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Running frontend linting in parallel"

  security-scan:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Running SAST scan in parallel"
