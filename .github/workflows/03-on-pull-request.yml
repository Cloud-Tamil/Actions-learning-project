name: 03 On Pull Request
on:
  pull_request:
    types: [opened, synchronize, reopened]

jobs:
  pr-check:
    runs-on: ubuntu-latest
    steps:
      - name: PR validation stub
        run: echo "PR #${{ github.event.pull_request.number }} ready for checks"
