name: 07 Multiple Events
on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

jobs:
  multi-event-job:
    runs-on: ubuntu-latest
    steps:
      - name: Detect triggering event
        run: echo "Workflow triggered via event: ${{ github.event_name }}"
