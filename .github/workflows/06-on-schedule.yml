name: 06 - Scheduled Cron
on:
  schedule:
    - cron: '0 4 * * 1-5' # 04:00 UTC Monday-Friday

jobs:
  scheduled-task:
    runs-on: ubuntu-latest
    steps:
      - name: Run Heartbeat
        run: echo "Scheduled cron triggered at $(date -u)"
