name: 31 Environments and Approvals
on: workflow_dispatch

jobs:
  deploy-prod:
    runs-on: ubuntu-latest
    # Requires configuration in Repository -> Settings -> Environments -> 'production'
    environment:
      name: production
      url: https://example.com/health
    steps:
      - name: Production deploy
        run: echo "Deployed to production after required approvals passed"
